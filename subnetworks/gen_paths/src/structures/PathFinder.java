package structures;

/**
 * A PathFinder, obviously, finds paths through a graph.
 * Each subtype will have its own requirements, but in general 
 * PathFinders will have a graph and Node- and/or EdgeLibraries.
 * 
 * @author chasman
 *
 */
public interface PathFinder {
	
 
}
